# LeafOS interfaces

- [LeafOS UI](leafos-ui/README.md): shared React/TypeScript client and local preview.

Interfaces communicate with Core through the LeafOS Protocol. Desktop host services (Tauri/Electron/browser) are a separate boundary from that protocol and from Core's execution adapters.
